#include <stdio.h>

int main(void)
{
	// char *s; // WRONG, Pointer unintialized...
	char s[1000];

	printf("Str: ");
	scanf("%s", str); // stops at whitespace e.g I/O, I: string one, O: string

	puts(s);

	return 0;
}